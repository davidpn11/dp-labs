{
  "name": "dplabs",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/davidpn11/dp-labs.git"
  },
  "bugs": {
    "url": "https://github.com/davidpn11/dp-labs/issues"
  },
  "scripts": {
    "clean:modules": "rimraf ./**/node_modules && yarn install",
    "build": "lerna run build",
    "web:dev": "lerna run --scope @dplabs/web dev --stream",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "lerna",
    "utils",
    "dplabs"
  ],
  "author": "David Nascimento",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.22.1",
    "rimraf": "^3.0.2",
    "typescript": "4.4.4"
  }
}
